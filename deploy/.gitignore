# =============================================================================
# Sub2API Deploy Directory - Git Ignore
# =============================================================================

# Data directories (generated at runtime when using docker-compose.local.yml)
data/
postgres_data/
redis_data/

# Environment configuration (contains sensitive information)
.env

# Backup files
*.backup
*.bak

# Temporary files
*.tmp
*.log
.prod-image-parts/
.prod-runtime-secret-*.tmp
4sub-prod-images-*.tar
4sub-prod-images-*.tar.gz
sub2api-*.tar
sub2api-*.tar.gz

langfuse/backups/
langfuse/reverse-tunnel.log
langfuse/.reverse-tunnel.pid
langfuse/.ssh-tunnel/
